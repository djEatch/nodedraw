{
  "name": "websockets",
  "description": "websocket Node.js POC for Azure App Service",
  "version": "1.0.0",
  "license": "ISC",
  "author": "DJEatch",
    "engines": {
        "node": ">=8.9.1"
    },
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },

  "dependencies": {
    "express": "^4.16.2",
    "socket.io": "^2.0.4"
  }
}
